import { updateTapes, tapesElement } from '../scripts/header';
// const { updateTapes, tapesElement } = require('../scripts/header');

describe('tapes', () => {
  it('1. should start as 0', () => {
  });
});
